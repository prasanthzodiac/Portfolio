# Optional — only if you use Base44 backend / auth
# Copy to .env.local for local dev. On Vercel/Render, add these in the dashboard.
# VITE_BASE44_APP_ID=
# VITE_BASE44_APP_BASE_URL=
# VITE_BASE44_FUNCTIONS_VERSION=
